{% if sources|length == 0 %}
<p>You have no sources.</p>
{% else %}
<ul id="sources_list">
    {% for source in sources %}
        <li>{{ source.type }} {{ source.label }}</li>
    {% endfor %}
</ul>
{% endif %}
